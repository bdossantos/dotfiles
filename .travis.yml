---

language: python
sudo: false
python:
  - '2.7'
cache: pip
before_script:
  - mkdir -p "$HOME/bin"
  - wget -qO- 'https://storage.googleapis.com/shellcheck/shellcheck-v0.6.0.linux.x86_64.tar.xz' | tar -xJv
  - mv 'shellcheck-v0.6.0/shellcheck' "$HOME/bin/shellcheck"
  - chmod + "$HOME/bin/shellcheck"
  - export PATH=$HOME/bin:$PATH
script:
  - make test
